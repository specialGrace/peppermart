import React from "react";
import Layout from "../../components/VendorDashboard/Layout/Layout";
import Default from "../../components/VendorDashboard/Default/Default";
const DefaultScreen = () => {
  return (
    <Layout>
      <Default />
    </Layout>
  );
};

export default DefaultScreen;
