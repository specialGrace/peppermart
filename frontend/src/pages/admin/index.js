import React from "react";
import Default from "../../components/AdminDashboard/Default/Default";
import Layout from "../../components/AdminDashboard/Layout/Layout";

const DefaultScreen = () => {
  return (
    <Layout>
      <Default />
    </Layout>
  );
};

export default DefaultScreen;
